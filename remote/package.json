{
  "name": "agents-kanban-remote",
  "version": "1.0.0",
  "private": true,
  "description": "The relay that Agents Kanban pushes its board to — runs on Netlify, Cloudflare Workers or any Node server. Lift this folder into its own repository and deploy.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --check functions/board.mjs && node --check functions/board-core.mjs && node --check server.js && node --check worker.js && node --check public/board.js"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3"
  }
}
