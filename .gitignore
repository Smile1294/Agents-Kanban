node_modules/
dist/
dist-test/
*.vsix
.DS_Store

# v1 wrote board state here. Sessions now come from Claude Code; nothing
# belongs in the repo.
.kanban/

# Agent worktrees, when this extension is pointed at its own repository.
# Written automatically on first session; kept here so a fresh clone is covered.
/.agentskanban/

# Local env files and Claude Code project settings can hold credentials
# (settings.local.json, exported keys) — never track them.
.env
.env.*
.claude/
