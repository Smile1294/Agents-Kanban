{
  "name": "agents-kanban",
  "displayName": "Agents Kanban",
  "description": "A kanban board your coding agents run themselves \u2014 each task in its own git worktree. Powered by Claude Code.",
  "version": "0.0.1",
  "publisher": "Smile1294",
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=22.6.0"
  },
  "categories": [
    "AI",
    "SCM Providers",
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "agentsKanban",
          "title": "Agents Kanban",
          "icon": "media/board.svg"
        }
      ]
    },
    "views": {
      "agentsKanban": [
        {
          "type": "webview",
          "id": "agentsKanban.board",
          "name": "Board"
        }
      ]
    },
    "commands": [
      {
        "command": "agentsKanban.openBoard",
        "title": "Agents Kanban: Open Board",
        "icon": "$(link-external)"
      },
      {
        "command": "agentsKanban.openSettings",
        "title": "Agents Kanban: Settings \u2014 Agents, Backends and Logins"
      },
      {
        "command": "agentsKanban.init",
        "title": "Agents Kanban: Initialise Board In This Repo",
        "icon": "$(new-folder)"
      },
      {
        "command": "agentsKanban.stopTask",
        "title": "Agents Kanban: Stop Agent"
      },
      {
        "command": "agentsKanban.openWorktree",
        "title": "Agents Kanban: Open Session Worktree"
      },
      {
        "command": "agentsKanban.newSession",
        "title": "Agents Kanban: New Session",
        "icon": "$(add)"
      },
      {
        "command": "agentsKanban.archiveSession",
        "title": "Agents Kanban: Archive Session",
        "icon": "$(archive)"
      },
      {
        "command": "agentsKanban.deleteSession",
        "title": "Agents Kanban: Delete Session",
        "icon": "$(trash)"
      },
      {
        "command": "agentsKanban.toggleFocus",
        "title": "Agents Kanban: Toggle Full-Window Board",
        "icon": "$(screen-full)"
      },
      {
        "command": "agentsKanban.selectProvider",
        "title": "Agents Kanban: Select Model Provider",
        "icon": "$(plug)"
      },
      {
        "command": "agentsKanban.addProvider",
        "title": "Agents Kanban: Add Model Provider"
      },
      {
        "command": "agentsKanban.testProvider",
        "title": "Agents Kanban: Test Model Provider Connection"
      },
      {
        "command": "agentsKanban.refreshModels",
        "title": "Agents Kanban: Refresh Model List",
        "icon": "$(sync)"
      }
    ],
    "configuration": {
      "title": "Agents Kanban",
      "properties": {
        "agentsKanban.worktreeRoot": {
          "type": "string",
          "default": "",
          "markdownDescription": "Where to create agent worktrees. A relative path resolves against the repository root. Empty means `.agentskanban/worktrees` inside the repository, which is added to `.gitignore` automatically. Point it outside the repository and nothing is written to your `.gitignore`."
        },
        "agentsKanban.focusMode": {
          "type": "string",
          "enum": [
            "off",
            "wide",
            "zen"
          ],
          "enumDescriptions": [
            "Never change your layout.",
            "Hide the panel and the secondary side bar while the board is in front, and restore them when you switch away.",
            "Use VS Code's Zen Mode, which also hides the activity and status bars."
          ],
          "default": "wide",
          "description": "How much of the window the board takes while it is in front. The primary side bar is never touched \u2014 it is how you get back."
        },
        "agentsKanban.sideBarHome": {
          "type": "string",
          "default": "workbench.view.explorer",
          "description": "Which view the left side bar is handed back to after you press the Kanban icon. VS Code answers a click on an activity-bar icon by showing that extension's view, and offers no way to decline; this is what puts yours back. Any 'workbench.view.*' command id works \u2014 workbench.view.explorer, workbench.view.scm, workbench.view.search, workbench.view.extension.<id>. Leave it empty to let the side bar stay on the Kanban control instead."
        },
        "agentsKanban.closeOnClickAway": {
          "type": "boolean",
          "default": true,
          "description": "Close the board when you click away from it \u2014 open a file, switch to another tab \u2014 putting the terminal and the right-hand chat back. Turn this off to make the board a pure toggle that only the icon, Ctrl+Alt+K or its tab's X can close, which is what you want if you like to glance at a file and come straight back."
        },
        "agentsKanban.hideSessionsOlderThanDays": {
          "type": "number",
          "default": 30,
          "minimum": 0,
          "markdownDescription": "Hide sessions this board has never touched once they are older than this many days. Another agent's session store can be global to the machine rather than per project — Codex's is — so opening a repository you used months ago otherwise adopts every old session it can match at once. Sessions the board has started, moved, tagged or archived are always shown, however old, and search always finds every session regardless of this setting. Set to `0` to adopt everything."
        },
        "agentsKanban.model": {
          "type": "string",
          "default": "claude-opus-5",
          "description": "Model for agent sessions."
        },
        "agentsKanban.maxConcurrentAgents": {
          "type": "number",
          "default": 3,
          "description": "How many agent sessions may run at once."
        },
        "agentsKanban.permissionMode": {
          "type": "string",
          "enum": [
            "default",
            "acceptEdits",
            "bypassPermissions",
            "plan",
            "dontAsk",
            "auto"
          ],
          "default": "acceptEdits",
          "description": "Permission mode for agent sessions. Agents run in isolated worktrees, so acceptEdits is a reasonable default."
        },
        "agentsKanban.notifyOnReview": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when an agent moves its card into a review column \u2014 its way of saying the work is ready for you to test."
        },
        "agentsKanban.claudeExecutable": {
          "type": "string",
          "default": "",
          "description": "Path to the `claude` executable. Leave empty to auto-detect (PATH, then the usual install locations).",
          "scope": "machine"
        },
        "agentsKanban.statusBar": {
          "type": "boolean",
          "default": true,
          "description": "Show an Agents Kanban item in the status bar. A second way in, alongside the activity-bar icon and Ctrl+Alt+K, and the one that shows how many agents are running."
        },
        "agentsKanban.runCommand": {
          "type": "string",
          "default": "",
          "markdownDescription": "Shell command that starts the app, run inside the session's worktree. Leave empty to detect it \u2014 a per-worktree launcher (`wt`) is preferred, then Laravel, then a `package.json` script. `${worktree}` is replaced with the worktree's path."
        },
        "agentsKanban.runUrl": {
          "type": "string",
          "default": "",
          "markdownDescription": "URL to open once `agentsKanban.runCommand` is serving, e.g. `http://localhost:3000`. Leave empty to use the port the detected launcher assigned. Only used alongside a custom `runCommand`."
        },
        "agentsKanban.provider": {
          "type": "string",
          "default": "inherit",
          "markdownDescription": "Id of the provider profile agent sessions run on, from `#agentsKanban.providers#`. The default, `inherit`, changes nothing: the `claude` CLI resolves its own provider from the environment, so an existing Bedrock or gateway setup keeps working. Switch it from the board's provider picker, or with **Agents Kanban: Select Model Provider**."
        },
        "agentsKanban.providers": {
          "type": "array",
          "default": [],
          "markdownDescription": "Model backends agent sessions can run on. Each profile compiles to environment variables on the `claude` process \u2014 the same variables the CLI documents \u2014 so anything Claude Code supports works here. **Credentials are never stored in settings**: they live in VS Code's secret storage, and `hasCredential` only records that one is set. Use **Agents Kanban: Add Model Provider** rather than editing this by hand. See [docs/PROVIDERS.md](https://github.com/Smile1294/Agents-Kanban/blob/main/docs/PROVIDERS.md).",
          "items": {
            "type": "object",
            "required": [
              "id",
              "kind"
            ],
            "properties": {
              "id": {
                "type": "string",
                "description": "Stable id. Referenced by agentsKanban.provider and used as the secret-storage key."
              },
              "label": {
                "type": "string",
                "description": "What the picker shows."
              },
              "kind": {
                "type": "string",
                "enum": [
                  "inherit",
                  "anthropic",
                  "bedrock",
                  "vertex",
                  "foundry",
                  "anthropicAws",
                  "gateway"
                ],
                "enumDescriptions": [
                  "Change nothing \u2014 the CLI resolves its own provider from the environment.",
                  "Anthropic's API: your Claude subscription, or a Console API key.",
                  "Amazon Bedrock, through the AWS SDK credential chain.",
                  "Google Cloud's Agent Platform (formerly Vertex AI).",
                  "Microsoft Foundry.",
                  "Claude Platform on AWS, billed through AWS Marketplace.",
                  "Anything serving the Anthropic Messages API at a URL: an LLM gateway, a self-hosted proxy, or a local model behind a translation proxy."
                ],
                "description": "Which backend this profile selects."
              },
              "baseUrl": {
                "type": "string",
                "description": "ANTHROPIC_BASE_URL for a gateway; the per-provider endpoint override otherwise."
              },
              "resource": {
                "type": "string",
                "description": "Microsoft Foundry resource name, when baseUrl is not given."
              },
              "region": {
                "type": "string",
                "description": "AWS_REGION for Bedrock, CLOUD_ML_REGION for Vertex."
              },
              "projectId": {
                "type": "string",
                "description": "ANTHROPIC_VERTEX_PROJECT_ID."
              },
              "regionPrefix": {
                "type": "string",
                "enum": [
                  "us",
                  "eu",
                  "apac",
                  "jp",
                  "au",
                  "global"
                ],
                "description": "ANTHROPIC_BEDROCK_REGION_PREFIX \u2014 which cross-region inference profile prefix to try first."
              },
              "workspaceId": {
                "type": "string",
                "description": "ANTHROPIC_AWS_WORKSPACE_ID, for Claude Platform on AWS."
              },
              "authStyle": {
                "type": "string",
                "enum": [
                  "bearer",
                  "apiKey"
                ],
                "enumDescriptions": [
                  "Send the credential as Authorization: Bearer (ANTHROPIC_AUTH_TOKEN). The default, and what most gateways want.",
                  "Send the credential as x-api-key (ANTHROPIC_API_KEY)."
                ],
                "description": "Which header the credential goes in. The wrong one reaches the gateway in a header it does not read, and returns 401."
              },
              "hasCredential": {
                "type": "boolean",
                "description": "Set automatically when a credential is stored in secret storage. Not the credential itself."
              },
              "credentialFromEnv": {
                "type": "string",
                "description": "Read the credential from this environment variable instead of secret storage."
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Extra request headers, sent as ANTHROPIC_CUSTOM_HEADERS."
              },
              "gatewayAuth": {
                "type": "boolean",
                "description": "The gateway authenticates upstream itself, so the CLI sends no SigV4 signature or key of its own (CLAUDE_CODE_SKIP_*_AUTH)."
              },
              "disableBetas": {
                "type": "boolean",
                "description": "Stop sending Anthropic's experimental anthropic-beta header values. The documented fix for 'Unexpected value(s) for the anthropic-beta header', which most translation proxies return."
              },
              "disableNonessentialTraffic": {
                "type": "boolean",
                "description": "Set CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1, as the gateway documentation advises."
              },
              "models": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Model ids to OFFER in the composer, out of what the backend serves. Leave it out and every model the endpoint reports is offered; set it to pin a few out of a long catalogue \u2014 OpenRouter serves over four hundred. Off first-party it is also how you name ids we cannot derive: Bedrock wants us.anthropic.claude-opus-5. Ids the endpoint does not serve are reported rather than silently honoured, because this extension used to write the WRONG ones here \u2014 Claude Code's own aliases, saved onto a profile pointed at something else."
              },
              "smallModel": {
                "type": "string",
                "description": "The model Claude Code should use for its OWN background work \u2014 naming a session, and other errands it never shows you. On a backend that does not serve Claude's models those requests name a model that does not exist and fail silently for the life of every session. Sets both ANTHROPIC_DEFAULT_HAIKU_MODEL and ANTHROPIC_SMALL_FAST_MODEL. Leave empty on a backend that serves Claude's models."
              },
              "contextWindow": {
                "type": "number",
                "description": "Context window for this profile's models, so the context meter has a denominator when the model is not one we know."
              },
              "env": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Extra environment variables, applied last. The escape hatch for anything not covered above."
              }
            },
            "additionalProperties": false
          }
        },
        "agentsKanban.discoverModels": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Ask the backend which models it can actually run, instead of using a built-in list. For a custom endpoint (`kind: gateway`) that means the ENDPOINT itself, over `GET <baseUrl>/v1/models` \u2014 not the `claude` CLI, whose answer is about Claude Code however `ANTHROPIC_BASE_URL` is pointed, which is how a DeepSeek profile came to offer `sonnet` and `haiku`. It is also where the context window and per-token price shown beside each model come from, and so where the spend figure on a non-Anthropic session comes from. For every other backend it is the `claude` CLI. This is what puts newly released models \u2014 and the ones your provider serves \u2014 in the picker without waiting for an extension update, and it is where the per-model effort levels and extended-thinking support come from. The answer is cached per provider; refresh it with **Agents Kanban: Refresh Model List**. Turn this off to use the built-in list only."
        },
        "agentsKanban.runtime": {
          "type": "string",
          "enum": [
            "claude",
            "codex"
          ],
          "enumDescriptions": [
            "Claude Code. Its backend is chosen by a provider profile.",
            "Codex. Uses the login already on this machine \u2014 a ChatGPT subscription or an OpenAI API key. No proxy."
          ],
          "default": "claude",
          "markdownDescription": "Which agent program new sessions run on. Each session keeps the one it started on, so a Claude Code card and a Codex card can run side by side. Change it per session on the composer bar, or for good here."
        },
        "agentsKanban.codexExecutable": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the `codex` binary. Leave empty to find it on your PATH, then in the usual install locations. Never resolved from `node_modules` \u2014 the board runs the Codex you maintain, not a bundled copy.",
          "scope": "machine"
        },
        "agentsKanban.orchestration": {
          "type": "string",
          "enum": [
            "minimal",
            "balanced",
            "maximum"
          ],
          "enumDescriptions": [
            "Prefer one agent. It may still split work that is genuinely unrelated.",
            "Split when the pieces are independent and worth testing separately.",
            "Split readily, up to four agents. A small task still stays one."
          ],
          "default": "balanced",
          "markdownDescription": "How eagerly a session should break its work into subtasks with `split_task`. This is the DEFAULT for new sessions; each card carries its own, chosen on the composer bar beside the model. It biases the agent rather than dictating a number: a small task stays one agent at **Maximum**, and a genuinely unrelated pair still splits at **Minimal**. The limit of four subtasks, one level deep, is unchanged."
        },
        "agentsKanban.whisperPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to `whisper-cli` (a whisper.cpp build). Leave empty to find it on your PATH. This is what makes the composer's microphone button dictate, and dictation is **fully local** — the audio never leaves your machine. Install whisper.cpp with: `brew install whisper-cpp` (macOS) or `sudo apt install whisper-cpp` (Debian/Ubuntu), then download a model with `whisper-cli -m models/ggml-base.en.bin -f nothing.wav` — or set `#agentsKanban.whisperModel#` to a model file you already have.",
          "scope": "machine"
        },
        "agentsKanban.whisperModel": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to a whisper ggml model file (`ggml-*.bin`), e.g. `/models/ggml-base.en.bin`. Download one from the whisper.cpp releases: `ggml-base.en.bin` is a good first model (English, ~150 MB). The settings page shows where to put it."
        },
        "agentsKanban.ffmpegPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to `ffmpeg`. Leave empty to find it on your PATH. ffmpeg captures the microphone (16 kHz mono WAV) for local transcription; install it with `brew install ffmpeg` or `sudo apt install ffmpeg`.",
          "scope": "machine"
        },
        "agentsKanban.recordDevice": {
          "type": "string",
          "default": "",
          "markdownDescription": "The microphone ffmpeg records from. Empty means the platform default: `default` (ALSA) on Linux, `:0` on macOS. **Windows cannot guess** — set a device NAME here (list them with `ffmpeg -f dshow -list_devices true -i dummy`) or the mic reports that it cannot record."
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "agentsKanban.openBoard",
          "when": "view == agentsKanban.board",
          "group": "navigation"
        },
        {
          "command": "agentsKanban.newSession",
          "when": "view == agentsKanban.board",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "agentsKanban.openBoard",
        "key": "ctrl+alt+k",
        "mac": "cmd+alt+k"
      }
    ]
  },
  "scripts": {
    "build": "node scripts/preflight.mjs && node esbuild.mjs",
    "watch": "node scripts/preflight.mjs && node esbuild.mjs --watch",
    "typecheck": "node scripts/preflight.mjs && node scripts/run-bin.mjs typescript tsc --noEmit && node scripts/run-bin.mjs typescript tsc --noEmit -p tsconfig.bench.json",
    "test": "node scripts/preflight.mjs && node scripts/test.mjs",
    "package": "node scripts/preflight.mjs && node scripts/run-bin.mjs @vscode/vsce vsce package",
    "smoke": "node scripts/preflight.mjs && node smoke.mjs",
    "verify": "node scripts/preflight.mjs && npm run typecheck && npm run build && npm run test && node scripts/check-contract.mjs && npm run smoke",
    "verify:package": "node scripts/preflight.mjs && npm run build && node test/package.test.mjs",
    "screenshots": "node scripts/preflight.mjs && node test/screenshots.mjs",
    "remote": "node scripts/preflight.mjs && npm run build && node server/server.mjs",
    "install-local": "node scripts/preflight.mjs && node scripts/install-local.mjs",
    "preflight": "node scripts/preflight.mjs --verbose",
    "icon": "node scripts/preflight.mjs && node scripts/make-icon.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.259",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.24.0",
    "playwright": "^1.49.1",
    "typescript": "^5.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Smile1294/Agents-Kanban.git"
  },
  "relayRepo": "https://github.com/Smile1294/agents-kanban-relay",
  "icon": "media/icon.png",
  "keywords": [
    "kanban",
    "agent",
    "claude",
    "worktree",
    "ai",
    "board",
    "automation"
  ],
  "galleryBanner": {
    "color": "#1f2430",
    "theme": "dark"
  },
  "bugs": {
    "url": "https://github.com/Smile1294/Agents-Kanban/issues"
  },
  "homepage": "https://github.com/Smile1294/Agents-Kanban#readme",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "This extension runs coding agents in this folder \u2014 they edit files, run commands and spend money. There is nothing it could usefully do in a folder whose authors you have not trusted."
    }
  }
}
