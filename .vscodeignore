.vscode/**
.github/**
# Agent worktrees are whole checkouts of this repository. vsce packages
# everything not listed here, so one live session would put the entire repo
# inside the .vsix, twice over.
.agentskanban/**
src/**
test/**
node_modules/.bin/**
esbuild.mjs
smoke.mjs
tsconfig.json
.gitignore
# Screenshots are for the repo, not for the installed extension — the
# Marketplace listing pulls them from GitHub by absolute URL instead.
docs/screenshots/**

# Development documentation. Useful in the repo, ~150KB on every user's disk if
# it ships. README.md and LICENSE are added by vsce regardless of this file.
docs/**
CLAUDE.md
PLAN.md
# The remote/ folder is a LIFT-OUT: the user copies it into its own repository
# and deploys it to Netlify. It is not part of the extension, and it carries
# its own package.json — shipping it would pad every install with code that
# never runs in VS Code.
remote/**
**/*.map
**/*.ts
**/*.vsix

# The Agent SDK ships a ~190MB native binary per platform. We use the Claude Code
# CLI already on the machine instead (see resolveClaudeExecutable), which keeps
# the extension small and lets the CLI update on its own schedule.
node_modules/@anthropic-ai/claude-agent-sdk-*/**

# vsce pulls in dev dependencies' trees; none of these are needed at runtime.
node_modules/@vscode/**
node_modules/typescript/**
node_modules/esbuild/**
node_modules/@esbuild/**
node_modules/@types/**
